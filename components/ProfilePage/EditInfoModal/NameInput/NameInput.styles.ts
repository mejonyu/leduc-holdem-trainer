import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  input: {
    width: "100%",
    borderWidth: 1,
    borderColor: "#cdcdcd",
    borderRadius: 3,
    padding: 14,
    marginBottom: 16,
  },
  invalidInput: {
    borderColor: "#fcd2cd",
  },
});

export default styles;
